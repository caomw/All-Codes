#include<iostream>
using namespace std;
typedef long long int ll;

int main()
{
    std::ios::sync_with_stdio(0);
    ll a,maxi=0;
    cin>>a;

    ll arr[a+5];
    for(ll i =0;i<a;i++)
    {
        ll n,counti=1;
        cin>>n;

        if(n>maxi)
            maxi=n
    }

        for(ll i=1;i<maxi;i++)
        {
            if(n%i)
            counti++;
            arr[i]=counti;

            if(arr)
        }

        cout<<counti<<"\n";
}
