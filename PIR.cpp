#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main()
{
    std::ios::sync_with_stdio(0);
    ll t;
    cin>>t;

    while(t--)
    {
        ll n=6;
        for(ll i=0;i<n;i++)
            cin>>arr[i];

        double vol;
        vol=2*arr[0]*arr[1]+2*arr[0]*arr[1]

    }
}

