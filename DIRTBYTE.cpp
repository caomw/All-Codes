#include<iostream>
using namespace std;
typedef long long int ll;


ll arr[1000000005];
int main()
{
    std::ios::sync_with_stdio(0);

    ll n,k;
    cin>>n>>k;

    ll arr[k];
    for(ll i=0;i<k;i++)
        cin>>arr[i];

    for(ll)
}
